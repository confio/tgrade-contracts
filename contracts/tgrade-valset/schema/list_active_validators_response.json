{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ListActiveValidatorsResponse",
  "type": "object",
  "required": [
    "validators"
  ],
  "properties": {
    "validators": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/ValidatorInfo"
      }
    }
  },
  "definitions": {
    "Binary": {
      "description": "Binary is a wrapper around Vec<u8> to add base64 de/serialization with serde. It also adds some helper methods to help encode inline.\n\nThis is only needed as serde-json-{core,wasm} has a horrible encoding for Vec<u8>",
      "type": "string"
    },
    "HumanAddr": {
      "type": "string"
    },
    "ValidatorInfo": {
      "description": "Operators SDK address, Tendermint public key, and tendermint voting power.",
      "type": "object",
      "required": [
        "operator",
        "power",
        "validator_pubkey"
      ],
      "properties": {
        "operator": {
          "$ref": "#/definitions/HumanAddr"
        },
        "power": {
          "description": "The voting power in Tendermint sdk",
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "validator_pubkey": {
          "description": "TODO: better name to specify this is the Tendermint pubkey for consensus?",
          "allOf": [
            {
              "$ref": "#/definitions/Binary"
            }
          ]
        }
      }
    }
  }
}
